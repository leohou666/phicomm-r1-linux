diff --git a/common/spl/spl_fit.c b/common/spl/spl_fit.c
index 18bff7b8..e6ed3fa8 100644
--- a/common/spl/spl_fit.c
+++ b/common/spl/spl_fit.c
@@ -914,6 +914,7 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 	/* Now check if there are more images for us to load */
 	for (; ; index++) {
 		uint8_t os_type = IH_OS_INVALID;
+		int fdt_ret;
 
 		node = spl_fit_get_image_node(&ctx, "loadables", index);
 		if (node < 0)
@@ -927,7 +928,7 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 		if (firmware_node == node)
 			continue;
 
-		image_info.load_addr = 0;
+		memset(&image_info, 0, sizeof(image_info));
 		ret = load_simple_fit(info, offset, &ctx, node, &image_info,
 				      CONFIG_SYS_BOOTM_LEN);
 		if (ret < 0 && ret != -EBADSLT) {
@@ -943,7 +944,14 @@ int spl_load_simple_fit(struct spl_image_info *spl_image,
 			debug("Loadable is %s\n", genimg_get_os_name(os_type));
 
 		if (os_takes_devicetree(os_type)) {
-			spl_fit_append_fdt(&image_info, info, offset, &ctx);
+			fdt_ret = spl_fit_append_fdt(&image_info, info, offset,
+						     &ctx);
+			printf("FITF os=%u ret=%d addr=%p magic=%08x\n",
+			       os_type, fdt_ret, image_info.fdt_addr,
+			       image_info.fdt_addr ?
+			       fdt32_to_cpu(*(fdt32_t *)image_info.fdt_addr) : 0);
+			if (fdt_ret)
+				return fdt_ret;
 			spl_image->fdt_addr = image_info.fdt_addr;
		}

diff --git a/arch/arm/mach-rockchip/spl.c b/arch/arm/mach-rockchip/spl.c
--- a/arch/arm/mach-rockchip/spl.c
+++ b/arch/arm/mach-rockchip/spl.c
@@
 	ulong image_pos = binman_sym(ulong, payload, image_pos);
 	ulong size = binman_sym(ulong, payload, size);
+
+	R1_SPL_MARK('n');
@@
 	if (image_pos != BINMAN_SYM_MISSING && size != BINMAN_SYM_MISSING) {
 		header = map_sysmem(image_pos, 0);
+		R1_SPL_MARK('o');
 		if (image_get_magic(header) == FDT_MAGIC) {
+			R1_SPL_MARK('p');
 			memmove(map_sysmem(addr, 0), header, size);
+			R1_SPL_MARK('P');
@@
 	if (image_get_magic(header) == FDT_MAGIC) {
 		struct spl_load_info load;

+		R1_SPL_MARK('Q');
